const repository = require('../repository');

/**
 * 
 * @param {*} req
 * @param {*} res
 */
exports.getNewsList = async (sql) => {
    return 1
}

/**
 * 
 * @param {*} req 
 * @param {*} res 
 */
exports.getNewsDetail = async (sql) => {
    return 1
}
